
// 1. Write a greeter function that asks for first and last name and greets the person

// 2. Write a numeros() that asks for 2 numbers.  Log multiplication of numbers

// 3. evenOrOdd() asks for a number and says whether its even or odd

// 4. allCaps() asks for a string and returns allCaps if str.length > 10

// 5. age() for loop asks for an age and spits out ages until person is 50

// 6. function that computes and returns the factorial for a number using for loop

// 7. insert a while loop into a function that asks for two numbers and returns a random number btw
//    the two numbers.  While loop has function guessing until result is > 2

// 8. turn factorial into a recursion 

// 9. log all of the even values from the arr1 = [1,3,6,11,4,2,4,9,17,16]

// 10. return all the even values from a list of lists arr1 = [1,3,[6,,7,8],[11,4,2],[4,9,17,16]]



// 11. use map function to create a new array that contains one element for each element
// in the original...if even, return 'even' else 'odd'

// 12. use filter method to return a new array with all the integers
//     hint: Number.isInteger(value)


// 13. use map and filter to determine the lengths of al the elements in an array of string
// values, then discard the even values


// 14. use reduce to compute the sum of the squares of all the numbers in an array


// 15. without useing a for,while or do/while loop, write some code that checks 
// whether the number 3 appears inside these arrays
// const numbers = [1,-1,2,3]

// #16 use reduce to add up the strings in this list
// let arr = ['a','abcd','abcde','abc','ab']

// #17
// write some code to extract the word 'mem' from the following nested array

// #18
// create an array object

// #19
// create an arry from the keys of obj1, don't mutate

// #20 
// create a new object named myObj that uses myProtoObjas its prototype
// add qux = 3 as new key pair

// #21 
// 1) iterate over keys via forEach versus 2) for...in 
// 3) use for...in with .hasOwnProperty
// Explain the difference

// #22
// create a fuction that creates and returns a copy of an object
// takes two arguments(object to copy and an array of keys that you want to copy)